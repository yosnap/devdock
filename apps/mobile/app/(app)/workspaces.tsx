// Workspaces tab entry point — delegates to WorkspacesScreen

import { WorkspacesScreen } from '../../src/screens/workspaces-screen';

export default function WorkspacesTab() {
  return <WorkspacesScreen />;
}
